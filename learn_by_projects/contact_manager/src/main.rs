use std::io;

struct ContactDetails{
    name: String,
    number: String,
    email: String,
}

struct ContactManager{
    contacts: Vec<ContactDetails>,
}

impl ContactDetails{
    fn new(contacts: Vec<ContactDetails>)-> self{
        ContactManager{contacts: vec::new()}
    }
    fn display(&self){
        println!("The number is: ",self.name);
    }
    fn 
}


fn main() {
    println!("Hello, world!");
}
